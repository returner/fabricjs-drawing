export class ShapeStroke {
    strokeWidth : number;
    stroke : string;
    constructor(strokeWidth : number = 1, stroke : string = "black"){
        this.strokeWidth = strokeWidth;
        this.stroke = stroke;
    }
}